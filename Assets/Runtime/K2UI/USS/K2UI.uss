@import url('project://database/Assets/Runtime/K2UI/USS/ToggleButton.uss?fileID=7433441132597879392&guid=1b6a73cd9e0a617469e5d501d696c7be&type=3#ToggleButton');
@import url('project://database/Assets/Runtime/K2UI/USS/compas.uss?fileID=7433441132597879392&guid=e2bd53ecacfb99a4f9c9d2b73a0b1303&type=3#compas');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Slider.uss?fileID=7433441132597879392&guid=3d8e4d9f1386ac94c97e6381ad01c59b&type=3#K2Slider');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Toggle.uss?fileID=7433441132597879392&guid=a6dc5024ba8ad204e9099eca78c0703f&type=3#K2Toggle');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Tabs.uss?fileID=7433441132597879392&guid=51945eb7b64f1ab4986c6d98dc37254e&type=3#K2Tabs');
@import url('project://database/Assets/Runtime/K2UI/USS/Ids.uss?fileID=7433441132597879392&guid=05eca1a4a31465746b0e8b392ee7cb7a&type=3#Ids');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Slider.uss?fileID=7433441132597879392&guid=3d8e4d9f1386ac94c97e6381ad01c59b&type=3#K2Slider');
@import url('project://database/Assets/Runtime/K2UI/USS/ToggleButton.uss?fileID=7433441132597879392&guid=1b6a73cd9e0a617469e5d501d696c7be&type=3#ToggleButton');
@import url('project://database/Assets/Runtime/K2UI/USS/compas.uss?fileID=7433441132597879392&guid=e2bd53ecacfb99a4f9c9d2b73a0b1303&type=3#compas');
@import url('project://database/Assets/Runtime/K2UI/USS/Graph.uss?fileID=7433441132597879392&guid=4652cfda17384d74abea95b5b7b4570c&type=3#Graph');
@import url('project://database/Assets/Runtime/K2UI/USS/Ids.uss?fileID=7433441132597879392&guid=05eca1a4a31465746b0e8b392ee7cb7a&type=3#Ids');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Slider.uss?fileID=7433441132597879392&guid=3d8e4d9f1386ac94c97e6381ad01c59b&type=3#K2Slider');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Toggle.uss?fileID=7433441132597879392&guid=a6dc5024ba8ad204e9099eca78c0703f&type=3#K2Toggle');
@import url('project://database/Assets/Runtime/K2UI/USS/K2Tabs.uss?fileID=7433441132597879392&guid=51945eb7b64f1ab4986c6d98dc37254e&type=3#K2Tabs');
@import url('project://database/Assets/Runtime/K2UI/USS/ToggleButton.uss?fileID=7433441132597879392&guid=1b6a73cd9e0a617469e5d501d696c7be&type=3#ToggleButton');
:root {
    --k2-font-regular: url('project://database/Assets/Fonts/LiberationSans-Regular%20SDF.asset?fileID=11400000&guid=1aa6f9b14a5b16b42ab66d521ba3157c&type=2#LiberationSans-Regular SDF');
    --k2-font-title: url('project://database/Assets/Fonts/Orbitron-Black%20SDF.asset?fileID=11400000&guid=5239ed64b02a55342a920e51ecef2971&type=2#Orbitron-Black SDF');
    --k2-color-background: rgb(0, 0, 0);
    --k2-color-button-text: rgb(242, 242, 242);
    --k2-color-button-background: rgb(12, 12, 27);
    --k2-color-button-background-hover: rgb(19, 19, 44);
    --k2-background-frame: rgba(76, 79, 188, 0.1);
    --k2-hover_frame: rgb(16, 16, 37);
    --k2-active_frame: rgb(30, 32, 77);
    --k2-color-border: rgb(76, 80, 188);
    --k2-color-border-bottom: rgb(90, 94, 211);
    --k2-font-size: 14px;
    --k2-spacing: 0;
    --k2-color-textfield-border-focus: rgb(76, 80, 188);
    --k2-color-invalid: rgb(123, 0, 0);
}

.separator {
    border-color: var(--k2-color-border);
    border-top-width: 2px;
}


.unity-button {
    height: 20px;
    padding: 0;
    border-radius: 3px;
    border-width: 1px;
    border-color: var(--k2-color-border);
    border-bottom-color: var(--k2-color-border-bottom);
    border-bottom-width: 2px;
    background-color: var(--k2-color-button-background);
    margin: 2px 4px;
    padding-left: 4px;
    padding-right: 4px;
    margin-left: 2px;
    margin-right: 2px;
    color: var(--k2-color-button-text);
}

.unity-button:hover {
    background-color: var(--k2-color-button-background-hover);
}

.unity-button:active {
    background-color: var(--k2-active_frame);
}

.unity-button.big {
    height: 35px;
    font-size: 16px;
}

.main-frame {
    font-size: var(--k2-font-size);
    letter-spacing: var(--k2-spacing);
    word-spacing: var(--k2-spacing);
    width: 350px;
    min-height: auto;
    background-color: var(--k2-color-background);
    -unity-background-image-tint-color: var(--k2-color-background);
    border-radius: 12px;
    border-color: var(--k2-color-border);
    border-width: 1px;
    -unity-font-definition: var(--k2-font-regular);
    opacity: 1;
    padding: 10px;
    border-bottom-width: 2px;
    border-radius: 10px;
    border-bottom-color: var(--k2-color-border-bottom);
}

/* Retro/astromech pass: this was the source of the "very big and clunky" dropdown look across
   every tab - a 17px header on every single Foldout (top-level AND nested) plus a full,
   saturated 1px border box on all four sides made each one read as its own oversized modern
   card instead of a compact retro readout. Font size comes down to 13px and the border is
   dimmed to a faint outline instead of a bright purple box; padding/margins are tightened to
   match. This is global (K2UI.uss, not K2D2.uss) since every Foldout in the mod - Node's Turn/
   Warp/Burn/Experimental and Node Info/Advanced today, other tabs' foldouts later - shares this
   base class. */
/* Dropped the bordered box around each Foldout entirely (was rgba(76, 80, 188, 0.35) on all four
   sides) - it read as an "ugly dark blue" frame wrapping every dropdown. The stock Parts Manager
   reference doesn't box its collapsible groups either (FL-T800/COUPLING/UTILITY/ENGINE are just
   header bars sitting flush in the list, no border around each one) - the header bar's own
   background + the margin between Foldouts is enough separation without it. */
.unity-foldout {
    margin-left: 4px;
    margin-right: 4px;
    margin-top: 0;
    margin-bottom: 6px;
    border-width: 0;
    background-color: rgb(0, 0, 0);
}

/* "Category header" bar treatment, matching how the stock RCS Thruster reference panel labels
   its "// RCS THRUSTER" section: a header bar a shade lighter than the panel background (instead
   of the near-black button background it inherited before), bold lavender/purple caps-style text
   with wider letter-spacing. Unity UI Toolkit has no CSS text-transform, so the actual ALL CAPS
   text (and the "// " prefix) has to be typed into each Foldout's UXML text - done for Node's
   Turn/Warp/Burn/Experimental and Node Info/Advanced for now; other tabs' foldouts will pick up
   this same header-bar styling automatically once their text gets the same treatment later. */
.unity-foldout__toggle {
    background-color: rgb(35, 40, 52);
    margin-left: 0;
    margin-right: 0;
    margin-top: 0;
    margin-bottom: 0;
    padding-top: 1px;
    padding-bottom: 1px;
    padding-left: 2px;
    padding-right: 0;
}

.unity-foldout__toggle:hover {
    background-color: rgb(45, 51, 66);
}

/* Fixes the header bar reading wider/rounder than the content box beneath it. USS specificity is
   compared like CSS: KerbalUI.uss's own ".unity-foldout > .unity-foldout__toggle" rule (two class
   selectors) beats the single-class ".unity-foldout__toggle" rule above (one class selector)
   regardless of which stylesheet loads later, so the margin:0 already set above was losing to
   KerbalUI's stock "margin: 4px 0 2px 0; border-radius: 8px;" for this same element. Matching
   KerbalUI's compound selector exactly gives this rule equal specificity, so load order (K2UI.uss
   loads after KerbalUI.uss in every UXML) decides the tie in our favor instead. */
.unity-foldout > .unity-foldout__toggle {
    margin: 0;
    border-radius: 0;
}

.unity-foldout > Toggle:hover {
    background-color: var(--k2-hover_frame);
}

.unity-foldout > Toggle Label {
    font-size: 13px;
    letter-spacing: 1px;
    color: rgb(180, 176, 232);
    -unity-font-style: bold;
}

/* Static "category divider" header, for grouping controls inside a single dropdown without
   nesting a collapsible Foldout per group (matching the stock Parts Manager reference's
   "// REACTION WHEEL" bars, which separate sections within one scrollable panel rather than
   being their own accordion items). Same header-bar look as .unity-foldout's own header above
   (same background/color/letter-spacing) but on a plain Label with no chevron, and the "// "
   prefix is kept in the text for these since - unlike an actual dropdown title - that's what
   distinguishes "this is a non-collapsible section" from "this is a collapsible dropdown" at a
   glance. */
.k2-section-header {
    background-color: rgb(35, 40, 52);
    color: rgb(180, 176, 232);
    font-size: 12px;
    letter-spacing: 1px;
    -unity-font-style: bold;
    padding: 3px 6px;
    margin-top: 8px;
    margin-bottom: 4px;
}

/* Wraps content revealed/hidden under a toggle (e.g. Node's "Before" radio row showing
   start_before only while selected), Parts-Manager-style. A thin left border + indent marks it as
   nested under whatever toggle controls its visibility, instead of just appearing/disappearing
   as an unrelated-looking sibling row. */
.k2-nested-content {
    margin-left: 12px;
    margin-top: 2px;
    margin-bottom: 4px;
    padding-left: 8px;
    border-left-width: 1px;
    border-left-color: rgba(87, 95, 111, 0.4);
}

.console {
    font-size: 14px;
    color: rgb(185, 207, 219);
    -unity-font-definition: url('project://database/Assets/Fonts/Sax%20Mono.asset?fileID=11400000&guid=a6fc3433277ba9741b90d888f9ae4b53&type=2#Sax Mono');
    -unity-font-style: normal;
    white-space: normal;
    margin-top: 5px;
    margin-bottom: 5px;
}

.k2-status-line {
    font-size: 16px;
    /* The theme's monospace face. This previously named
       Theme/Fonts/NotoSansMonoCJK/NotoSansMonoCJKtc-Regular SDF, whose only difference here is
       that it is a Traditional-Chinese face - and because a TextCore FontAsset in Dynamic atlas
       mode must keep its source font file, naming it dragged that font's ~16 MB .otf into
       k2d2_ui.bundle as a build dependency. JetBrainsMono is the theme's other monospace face,
       already in the bundle for K2D2.uss, so the alignment contract is unchanged and the bundle
       does not grow. CJK still resolves at runtime through the fallback chain. */
    -unity-font-definition: url('project://database/Packages/uitkforksp2.controls/Assets/Theme/Fonts/JetBrainsMono/JetBrainsMono-Regular%20SDF.asset?fileID=11400000&guid=f5454c9c675002c44a5a7234bd40c390&type=2#JetBrainsMono-Regular SDF');
    white-space: normal;
}

.k2-status-line--normal {
    color: rgb(255, 255, 255);
}

.k2-status-line--warning {
    color: rgb(255, 202, 0);
}

.k2-status-line--error {
    color: rgb(255, 0, 0);
    -unity-font-style: normal;
}

/* Node tab "finish it up" pass: K2's own face replaces the redundant "K2:" text prefix that used
   to open every status line (stripped from the strings themselves in TurnTo.cs/BurnManeuvre.cs/
   WarpTo.cs/NodeExPilot.cs), and the yellow headline (.k2-status-line) + the plain-text detail
   readout underneath it (.console) sit directly on top of each other with no gap, read together
   as one continuous message from K2 instead of two separate-looking labels. */
.k2-status-row {
    flex-direction: row;
    align-items: center;
    /* Small breathing room so the icon doesn't sit flush against the panel's own left edge, same
       idea as the slider label row's own left/right padding above. */
    padding-left: 6px;
}

.k2-status-speech {
    flex-direction: column;
    flex-grow: 1;
}

/* Scoped to only the merged speech block above - .console keeps its normal 5px top margin
   everywhere else it's used (Node Info readouts, other tabs' own console-style panels, etc.). */
.k2-status-speech > .console {
    margin-top: 0;
}

.k2-status-speech > .k2-status-line {
    margin-bottom: 0;
}

/* k2d2_big_icon.png is the same sticker-style mascot face already used on the About tab and the
   window's own title icon - reused here at a smaller size as the "K2 is talking" avatar, with a
   wider gap to the speech text than a plain adjacent placement would give it (light positions
   below are rescaled to match this size). */
.k2-avatar {
    width: 48px;
    height: 48px;
    flex-shrink: 0;
    margin-right: 16px;
}

.k2-avatar-icon {
    width: 100%;
    height: 100%;
    background-image: url('project://database/Assets/Images/k2d2_big_icon.png?fileID=2800000&guid=8ef988e0f43beb149a76620d7ba3ed65&type=3#k2d2_big_icon');
    -unity-background-scale-mode: scale-to-fit;
    position: relative;
}

/* The 3 small grille lines already baked into the bottom-left of k2d2_big_icon.png's own artwork
   (measured directly off the 240x240 source PNG's pixels, then scaled to this 48px icon size) -
   K2Avatar.cs lights these up blue in sequence while the autopilot runs, and back off in reverse
   when it stops, so it reads as those exact lines glowing rather than a decoration bolted on top.
   Sized/positioned in flat px (not %) on purpose - a previous slider dash bug in this same game
   embedding traced back to a 1px-scale line rounding to an invisible sub-pixel sliver, so these
   stay comfortably thick (2px) rather than trying to match the artwork's thinner line weight
   exactly. */
.k2-avatar-light {
    position: absolute;
    left: 12px;
    width: 8px;
    height: 2px;
    background-color: rgba(0, 0, 0, 0);
    border-radius: 1px;
    transition-property: background-color;
    transition-duration: 0.15s;
}

.k2-avatar-light-0 { top: 41px; }
.k2-avatar-light-1 { top: 43px; }
.k2-avatar-light-2 { top: 45px; }

.k2-avatar-light--on {
    background-color: rgb(90, 190, 255);
}

Label {
    min-width: 50px;
    -unity-text-align: upper-left;
}

#unity-text-input {
    padding: 0;
    background-color: rgba(0, 0, 0, 0.76);
    border-color: var(--k2-color-border);
    border-radius: 2px;
    max-width: 100px;
    --unity-cursor-color: var(--k2-color-border);
}

.unity-text-field:focus .unity-base-text-field__input {
    border-color: var(--mnc-color-textfield-border-focus);
}

.unity-text-field-invalid > .unity-base-text-field__input {
    color: var(--mnc-color-invalid);
    border-color: var(--mnc-color-invalid);
}

.unity-text-field-invalid:focus > .unity-base-text-field__input {
    color: var(--mnc-color-invalid);
    border-color: var(--mnc-color-invalid);
}

.unity-base-field:focus Label {
    color: var(--mnc-color-invalid);
}

.unity-base-field Label {
    font-size: 14px;
}

#unity-text-input > TextElement {
    margin-left: 5px;
    margin-top: 0;
}

.unity-progress-bar__background {
    background-color: var(--k2-background-frame);
    border-radius: 10px;
    border-color: rgb(255, 255, 255);
    border-width: 2px;
}

.unity-progress-bar__progress {
    background-color: rgb(37, 176, 0);
    margin-left: -1px;
    border-radius: 11px;
    padding: 8px;
    margin-top: -1px;
    margin-bottom: -1px;
    margin-right: -1px;
}

.unity-progress-bar__title {
    display: none;
    font-size: 14px;
    color: rgb(255, 255, 255);
    -unity-font-style: bold;
}

.k2-progress-bar {
    min-height: 16px;
    margin-top: 4px;
    margin-bottom: 4px;
}

.k2-progress-bar #frame {
    width: 100%;
    height: 100%;
    background-color: var(--k2-background-frame);
    border-color: rgb(255, 255, 255);
    border-radius: 15px;
    border-width: 2px;
    flex-grow: 1;
    flex-direction: column;
    overflow: hidden;
    border-top-left-radius: 8px;
    border-bottom-left-radius: 8px;
    border-top-right-radius: 8px;
    border-bottom-right-radius: 8px;
    justify-content: center;
}

.k2-progress-bar #progress {
    width: 439px;
    position: absolute;
    height: 100%;
    min-width: initial;
    border-color: rgb(255, 255, 255);
    border-width: 0;
    min-height: initial;
    background-color: rgb(37, 176, 0);
    border-top-left-radius: 0;
    border-bottom-left-radius: 0;
    border-top-right-radius: 5px;
    border-bottom-right-radius: 5px;
}

.k2-progress-bar #label {
    color: rgb(255, 255, 255);
    -unity-font-style: bold;
    border-color: rgb(195, 19, 19);
    text-shadow: 0 0 1px rgb(0, 0, 0);
    position: absolute;
    width: 100%;
    font-size: 12px;
    -unity-text-align: middle-center;
    letter-spacing: 0;
}

.group_label {
    position: absolute;
    min-width: 0;
    font-size: 12px;
    top: -9px;
    left: 4px;
    height: 12px;
    padding-top: 2px;
    padding-right: 4px;
    padding-bottom: 2px;
    /* The legend masks the group's top border, so its background must match the panel it sits on,
       not the old black app shell. The game theme declares the panel #1e2129 = rgb(30, 33, 41)
       (references/UitkForKsp2-Unity/Assets/Theme/KerbalUI.uss:42). */
    background-color: rgb(30, 33, 41);
    padding-left: 6px;
    color: rgb(153, 160, 255);
}

.group {
    flex-grow: 1;
    background-color: rgba(0, 0, 0, 0);
    border-width: 1px;
    border-bottom-width: 2px;
    border-color: var(--k2-color-border);
    border-bottom-color: var(--k2-color-border-bottom);
    margin-top: 10px;
    padding-top: 5px;
    padding-bottom: 0;
    border-top-left-radius: 4px;
    border-bottom-left-radius: 4px;
    border-top-right-radius: 4px;
    border-bottom-right-radius: 4px;
    padding-left: 6px;
    padding-right: 12px;
}

.unity-popup-field:focus {
    color: rgb(255, 255, 255);
}

.unity-popup-field__label {
    width: 150px;
}

.unity-popup-field__label:focus {
}

.unity-popup-field__input.fixed {
    width: 200px;
}

.unity-popup-field__input {
    border-radius: 8px;
    border-width: 1px;
    border-color: var(--k2-color-border);
    border-bottom-color: var(--k2-color-border-bottom);
    padding: 1px;
    width: 200px;
    background-color: rgb(0, 0, 0);
    height: 100%;
}

.unity-base-popup-field__text {
    margin-left: 0;
    margin-right: 4px;
    margin-top: 0;
    margin-bottom: 0;
    color: rgb(255, 255, 255);
}

Dropdown > .unity-base-field__label {
    color: rgb(0, 85, 255);
}

.unity-base-dropdown__item:hover {
    background-color: rgba(255, 255, 0, 0.2);
    border-top-left-radius: 3px;
    border-bottom-left-radius: 3px;
    border-top-right-radius: 3px;
    border-bottom-right-radius: 3px;
}

.unity-base-dropdown__arrow {
    display: none;
}
